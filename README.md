# Docker container with ready to use zenoh rmw

Following repository contains ready to use dockerfile and the compose file that contains three services:
* router services
* listener
* talker

Both listener and talker uses the zenoh rmw.
